@{
    ViewData["Title"] = "Welcome";
    var numTimes = ViewData["NumTimes"] as int? ?? 0;
    var message = ViewData["Message"] as string ?? "Hello unknown";
}

<h2>Welcome</h2>

<ul>
    @for (int i = 0; i < numTimes; i++)
    {
        <li>@message</li>
    }
</ul>
